<%= form_with(model: coin, local: true) do |form| %>
  <% if coin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coin.errors.count, "error") %> prohibited this coin from being saved:</h2>

      <ul>
      <% coin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label t('links.description') %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label t('links.acronym') %>
    <%= form.text_field :acronym %>
  </div>

  <div class="field">
    <%= form.label t('links.url_image') %>
    <%= form.text_field :url_image %>
  </div>

  <div class="field">
    <%= form.select("mining_type_id", @mining_types_options, { include_blank: t('links.select') }) %>
  </div>

  </div>

  <div class="actions">
    <%= form.submit t('links.create') %>
  </div>
<% end %>
